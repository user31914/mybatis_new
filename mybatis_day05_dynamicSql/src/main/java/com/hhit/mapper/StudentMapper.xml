<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hhit.mapper.StudentMapper">
    <sql id="condition">
        <if test="name!=null and name!=''">
            and name like #{name}
        </if>
        <if test="age!=null and age!= 0">
            and age = #{age}
        </if>
        <if test="gender!=null and gender!=''">
            and gender = #{gender}
        </if>
    </sql>
    <select id="selectByCondition" parameterType="student" resultType="student">
        select * from student where 1 = 1
       <include refid="condition"></include>
    </select>

    <select id="selectByConditionChoose" parameterType="student" resultType="student">
        select * from student where 1 = 1
       <choose>
           <when test="name!=null and name!=''">
               and name like #{name}
           </when>
           <when test="age!=null and age!= 0">
               and age = #{age}
           </when>
           <when test="gender!=null and gender!=''">
               and gender = #{gender}
           </when>
        <otherwise>
            order by age
        </otherwise>
       </choose>
    </select>
    <select id="selectByConditionWhere" parameterType="student" resultType="student">
        select * from student
        <where>
          <include refid="condition"></include>
        </where>
    </select>
<select id="selectByConditionTrim" parameterType="student" resultType="student">
        select * from student
        <trim prefix="where" prefixOverrides="and">
            <if test="name!=null and name!=''">
                and name like #{name}
            </if>
            <if test="age!=null and age!= 0">
                and age = #{age}
            </if>
            <if test="gender!=null and gender!=''">
                and gender = #{gender}
            </if>
        </trim>
    </select>
  <update id="update" parameterType="student">
      update student
      <!--      <set>-->
      <!--          <if test="name!=null and name != ''">-->
      <!--              name = #{name}-->
      <!--          </if>-->
      <!--          <if test="age!=null and age != 0">-->
      <!--              age = #{age}-->
      <!--          </if>-->
      <!--          <if test="gender!=null and gender != ''">-->
      <!--              gender = #{gender}-->
      <!--          </if>-->
      <!--      </set>-->
    <trim prefix="set" suffixOverrides=",">
        <if test="name != null and name!= ''">
            name = #{name},
        </if>
        <if test="age != null and age!= 0">
            age = #{age},
        </if>
        <if test="gender != null and gender!= ''">
            gender = #{gender},
        </if>
    </trim>
      where id = #{id}
  </update>

    <delete id="delete">
        delete from student
<!--        <where>-->
<!--            <foreach collection="array" item="id" open=" and id in (" close=")" separator=",">-->
<!--                #{id}-->
<!--            </foreach>-->
<!--        </where>-->
        <where>
            <foreach collection="array" item="id" open="(" close=")" separator="or">
                id = #{id}
            </foreach>

        </where>
    </delete>
    <delete id="deleteList">
        delete from student
        <where>
            <foreach collection="list" item="id" open="and id in (" close=")" separator=",">
                #{id}
            </foreach>
        </where>
    </delete>
    <delete id="deleteByObject" parameterType="student">
        delete from student
      <where>
          <foreach collection="ids" item="id" open="and id in (" close=")" separator=",">
              #{id}
          </foreach>
      </where>
    </delete>

    <insert id="insert" >
        insert into student values
        <foreach collection="list" item="stu" separator=",">
            (default,#{stu.name},#{stu.age},#{stu.gender})
        </foreach>
    </insert>

    <select id="selectByName" parameterType="Stu" resultType="Stu">
        select * from student
<!--        <where>-->
<!--            <if test="name != null and name != ''">-->
<!--               and name like concat('%',#{name},'%')-->
<!--            </if>-->
<!--        </where>-->
        <bind name="condition" value="'%'+name+'%'"/>
        <where>
            <if test="name != null and name != ''">
                and name like #{condition}
            </if>
        </where>
    </select>
</mapper>